---
- name: restart redis
  service:
    name: "{{ redis_daemon }}"
    state: restarted

- name: reload sysctl
  command: sysctl --system
  changed_when: true

- name: systemd daemon reload
  systemd:
    daemon_reload: true
